<ul class="comments collection">
    {{#each this}}
    <section class="comment collection-item">
        <div class="meta">
            {{user.username}} on {{format_date created_at}}
        </div>
        <div class="text">
            {{comment_text}}
        </div>
    </section>
    {{/each}}
</ul>